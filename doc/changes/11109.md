- Inline test libraries now produce aliases `inline-test-name_of_lib` allowing
  users to run specific inline tests as `dune build @inline-test-name_of_lib`.
  (#11109, partially adresses #10239, @Alizter)
