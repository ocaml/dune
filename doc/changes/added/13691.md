- Add `(dirs ...)` to `(include_subdirs (mode qualified) ...)`, allowing
  qualified source directories to be renamed in the module path, including
  when bootstrapping. Mappings can use variables, including reads from generated
  files outside the qualified group (#13691, #16418, #16420, fixes #7605,
  @anmonteiro).
