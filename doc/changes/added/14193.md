- Add `%{pkg:<package>:<section>}` pform for resolving package install
  directories in regular rules. This introduces an implicit dependency on the
  package and works with workspace packages, lock-file packages, and installed
  packages. (#14200, fixes #14193, fixes #3378, @Alizter)
