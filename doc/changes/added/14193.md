- Add `%{pkg:<package>:<section>:<path>}` pform for resolving individual
  package install files in regular rules. For workspace packages, this resolves
  to the original source file. This introduces an implicit dependency on the
  package and works with workspace packages, lock-file packages, and installed
  packages. (#14200, fixes #14193, fixes #3378, @Alizter)
