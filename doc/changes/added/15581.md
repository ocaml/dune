- `dune rules` now lists anonymous actions (actions attached to an alias),
  defaults to the `@default` alias when no target is given, and includes rule
  locations when `--with_locs` is passed. (#15581, @NatKarmios)
