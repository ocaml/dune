- Add a `(libraries ...)` field to instrumentation stanzas to declare libraries
  that are linked only when the corresponding instrumentation backend is active
  starting in Dune 3.25 (#15622, @nojb)
