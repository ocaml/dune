- Add the `(job_slots <action>)` field to `rule` stanzas. A rule that runs
  another build system can reserve more than one job slot, and its action sees
  the number in `DUNE_JOB_SLOTS`. (#16475, @orbitz)
