- Warn about duplicate dependencies in package definitions. Dune now detects
  and reports when the same dependency is listed multiple times in `depends`,
  `conflicts`, or `depopts` fields. (#12861, fixes
  #2678)
