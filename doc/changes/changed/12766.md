- Starting with version 3.21 of the Dune language, Dune no longer changes the
  default set of compiler warnings (as it did when building in the `dev`
  profile). For users that would like to keep the old behaviour, the special
  variable `%{dune-warnings}` can be used in the `(flags)` field.
  (#12766, @nojb)