- `(deps (package ...))` now exposes the declared packages and depends on the
  closure of the OCaml libraries they provide. Previously, every package in the
  workspace could be discoverable via the shared install staging area.
  Non-library entries and unrelated libraries remain available only from
  explicitly declared packages. (#14373, @Alizter)
