- `%{bin:NAME}` now expands with a leading `./` when the binary lives in
  the action's directory, so that shells like `bash` execute it directly
  instead of looking it up in `PATH`. (#15156, fixes #15147, @Alizter)
