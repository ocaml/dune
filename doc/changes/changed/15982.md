- Solve all distinct configured lock-directory platforms in one SAT problem
  and select the same package version across platforms. Repeated platform
  entries are ignored while preserving first-occurrence order. Solver failures
  report package unavailability and cross-platform version conflicts for each
  affected platform. The cross-platform version constraint was proposed by
  @art-w. (#15982, @Alizter)
