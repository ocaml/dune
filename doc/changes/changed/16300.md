- `dune describe workspace` no longer describes a Melange-only library as a
  `library` item reporting OCaml artifacts that are never built. It is now
  described by a `melange.library` item (#16300, @NatKarmios)
