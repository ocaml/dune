- Revert the comparison behavior of the `diff` action for non-existent files.
  Non-existent files are once again treated as empty for comparison purposes,
  restoring the documented behavior. (#13974, fixes #13891, @Alizter)
