- Fix internal-error crash when building a module artifact target
  (`%{cmi:...}`, `%{cmo:...}`, `%{cmx:...}`, `%{cmt:...}`, `%{cmti:...}`)
  under `(include_subdirs qualified)` when two qualified subdirectories
  contain a module with the same name. Modules in qualified
  subdirectories can now be referenced via their path, e.g.
  `%{cmi:sub_a/foo}` for the module `Sub_a.Foo`.
  (#14498, fixes #14496, @Alizter)
