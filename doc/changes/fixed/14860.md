- Allow the use of all the available variables in the `(enabled_if)` field in
  `(executable ...)` and `(install ...)` stanzas, similar to other stanzas like
  `(library ...)`. (#14860, fixes #14789, @punchagan)
