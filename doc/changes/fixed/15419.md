- Remove ppx runtime dependencies from the list of dependencies provided via
  the `root_module` field. If such a runtime dependency is needed, it should be
  written in the `libraries` field. (#15419, @rgrinberg)
