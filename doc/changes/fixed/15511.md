- Make `(deps (package ...))` include the workspace library closure in each
  compilation mode, including PPX runtimes, virtual libraries with or without
  defaults, serialized subsystem and instrumentation references, redirect
  chains, and deprecated-package metadata. This preserves installed library
  semantics without exposing complete owning
  packages or following the broader package dependency graph. (#15511,
  @Alizter)
