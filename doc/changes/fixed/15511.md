- Make `(deps (package ...))` depend on the compile and partial-link closure of
  the OCaml libraries provided by the package. This works for workspace,
  findlib, external dune-package, and Dune package-management libraries without
  following package dependencies or exposing unrelated libraries from their
  owning packages. (#15511, @Alizter)
