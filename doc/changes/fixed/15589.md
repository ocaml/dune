- Make `dune pkg enabled` query a running Dune RPC server, so it observes
  transient server-side `--pkg enabled` and `--pkg disabled` settings without
  trying to acquire the build lock (#15589, fixes #15587, @Alizter)
