- Fix stale-artifact bug where editing the `.mli` of a `(private_modules ...)`
  module did not trigger a rebuild of consumers, causing subsequent builds to
  fail with "inconsistent assumptions over interface" (#16071, @copilot)
