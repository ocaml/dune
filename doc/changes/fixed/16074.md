- Fix shared cache misses when the build directory is given as an absolute path
  that resolves to the default location (e.g. `DUNE_BUILD_DIR=$PWD/_build`).
  The path is now normalised to its relative form so that the cache key is the
  same regardless of how the directory is spelled. (#16074, fixes #16072,
  @Copilot)
