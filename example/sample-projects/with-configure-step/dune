(rule
 ((fallback)
  (targets (config))
  (deps    (config.defaults))
  (action  (copy %{<} %{targets}))))

(rule
 ((targets (config.full))
  (deps    (config_common.ml config))
  (action  (run %{OCAML} %{dep:real_configure.ml}))))
