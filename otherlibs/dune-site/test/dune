(cram
 (applies_to :whole_subtree)
 (deps
  (package dune)
  (package dune-site)))

(cram
 (applies_to run_2_9 run)
 (enabled_if
  (and
   (<> %{os_distribution} nixos)
   (not %{env:CI=false})
   (not %{env:NIX_CI=false}))))
