(library
 ((name configurator)
  (public_name jbuilder.configurator)
  (libraries (stdune ocaml_config str))
  (flags (:standard -safe-string (:include flags/flags.sexp)))
  (preprocess no_preprocessing)))

(jbuild_version 1)
