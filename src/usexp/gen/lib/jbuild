(library
 ((name gen_parsexp_lib)
  (libraries (base))
  (preprocess (pps (ppx_sexp_conv
                    ppx_compare
                    ppx_enumerate
                    ppx_hash
                    ppx_variants_conv)))))

(jbuild_version 1)
