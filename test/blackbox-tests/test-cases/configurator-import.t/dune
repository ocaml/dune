(rule
 (targets c_flags.sexp)
 (deps
  (:deps-conf configure/configure.exe))
 (action
  (run %{deps-conf})))

(executable
 (name configurator)
 (foreign_stubs
  (language c)
  (names my_stub)
  (flags
   :standard
   (:include c_flags.sexp))))
