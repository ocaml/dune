(jbuild_version 1)

(copy_files# lexers/*.ml{,i})
(copy_files# include/bar.h)

(library
 ((name foo)
  (c_names (bar))
  (modules ())
  (wrapped false)))

(executable
 ((name test)
  (libraries (foo))))

(alias
 ((name bar-source)
  (deps (bar.h))
  (action (echo "${read:bar.h}"))))
