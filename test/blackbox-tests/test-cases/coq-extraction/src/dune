(executable
  (name Bug_OCaml)
  (modes (best exe)))

; (install
;  (package Bug_OCaml)
;  (section bin)
;  (files (bug.exe as bug)))

(coq.extraction
	(theories Bug_Coq)
	(prelude extraction)
	(extracted_modules Extract))

