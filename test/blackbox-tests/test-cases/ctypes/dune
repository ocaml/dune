(cram
 (applies_to :whole_subtree)
 (deps
  libexample/libexample.a
  libexample/libexample.so
  libexample/example.h
  libexample/pkgconfig/libexample.pc
  %{bin:install}
  %{bin:awk}
  (package ctypes)))

(cram
 (applies_to :whole_subtree)
 (deps
  libneed-mangling/libneed-mangling.a
  libneed-mangling/libneed-mangling.so
  libneed-mangling/example.h
  libneed-mangling/pkgconfig/need-mangling.pc
  %{bin:install}
  %{bin:awk}
  (package ctypes)))

(cram
 (applies_to lib-return-errno.t)
 (deps
  (package integers)))

(cram
 (applies_to
  bytecode-stubs-external-lib.t
  lib-pkg_config.t
  lib-pkg_config-multiple-fd.t
  lib-external-name-need-mangling.t
  exe-pkg_config-multiple-fd.t
  lib-return-errno.t
  github-5561-name-mangle.t
  exe-pkg_config.t)
 (deps %{bin:pkg-config}))
