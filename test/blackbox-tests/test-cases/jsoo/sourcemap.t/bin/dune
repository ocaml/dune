(executable
  (name main)
  (libraries mylib)
  (modes js)
  (js_of_ocaml
    (flags (:standard \ --source-map-inline) --sourcemap)
    (link_flags (:standard \ --source-map-inline) --sourcemap)
  )
)