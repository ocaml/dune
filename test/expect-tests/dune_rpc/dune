(library
 (name dune_rpc_tests)
 (inline_tests)
 (libraries
  dune_util
  dune_rpc
  dune_rpc_server
  dune_rpc_client
  stdune
  test_scheduler
  csexp
  fiber
  ;; This is because of the (implicit_transitive_deps false)
  ;; in dune-project
  ppx_expect.config
  ppx_expect.config_types
  base
  ppx_inline_test.config)
 (preprocess
  (pps ppx_expect)))

(rule
 (with-stdout-to
  digests.mli
  (echo "")))

(rule
 (with-stdout-to
  dune_rpc_tests.mli
  (echo "")))

(rule
 (with-stdout-to
  long_polling.mli
  (echo "")))

(rule
 (with-stdout-to
  finalization.mli
  (echo "")))
