(library (
   (name mylib)
   (public_name mylib)
   (modules (good bad))
   (public_interfaces (:standard \ Bad))
))

(library (
   (name mylib_see_private)
   (public_name mylib.see_private)
   (modules (see_private))
   (libraries (mylib))
))
